import { expect, test } from "bun:test";
import { assessBrief, readDataset, validate } from "./runner";
test("pilot manifest pins materialized fixture bytes and all nine arms", () => { const dataset = readDataset(); expect(validate(dataset)).toEqual([]); expect(dataset.cells).toHaveLength(9); });
test("manifest rejects mutable brief and treatment drift", () => { const dataset = structuredClone(readDataset()); dataset.cells.find((cell) => cell.id === "quota-window-B")!.briefHash = "not-a-hash"; dataset.cells.find((cell) => cell.id === "quota-window-A")!.requestedEffort = "medium"; expect(validate(dataset)).toContain("quota-window-B: brief hash contract is invalid"); expect(validate(dataset)).toContain("quota-window: A/B model or effort mismatch"); });
test("brief admission rejects code and algorithm-complete handoff", () => { expect(assessBrief("Behavior Diagnosis Files Check Escalate\n```ts\nfunction solve() {}\n```")).toContain("brief contains executable handoff material"); expect(assessBrief("Behavior Diagnosis Files Check Escalate: first inspect, then edit, then return.")).toContain("brief contains algorithm-complete pseudocode"); });
