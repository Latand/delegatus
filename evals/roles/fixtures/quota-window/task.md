Repair case/quotaSummary.ts. quotaSummary(Input) returns {session: number|null, weekly: number|null} used percentages. Primary/secondary are transport slots; use the declared horizons through support/routeWindowsByHorizon.ts. Weekly-only, reversed, dual and absent windows must work; declared horizon wins over slot, capturedAt fallback applies, zero is valid, and input must remain unchanged. Preserve the interface. Edit only case/quotaSummary.ts and case/quotaSummary.test.ts. Run the supplied public behavioral vectors and your tests. No network, production operations, history searches, sibling or grader access. Public check command: bun check.ts.
