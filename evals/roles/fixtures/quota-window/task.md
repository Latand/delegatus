# Quota-window summary

Correct the synthetic summary so the declared horizon wins over slot position. Preserve unknown values and input immutability. Change only `case/quotaSummary.ts` and its test.
