Repair case/diagnosticSummary.ts. diagnosticSummary({label,text,limit}) returns {label,text,truncated}. Sanitize the full label and text through support/redactMonitorText.ts before limiting text by Unicode code points (limit is a positive integer). Trim text, reserve the last code point for … when truncated, retain useful labels, and report truncation truthfully. Synthetic credentials, emails, encoded home paths and authorization/cookie values must be removed according to the pinned sanitizer contract. Empty/Unicode/hostile inputs remain data, input stays unchanged. Edit only case/diagnosticSummary.ts and case/diagnosticSummary.test.ts. Run public vectors and your tests. No network, arbitrary-file/transcript reads, production operations, history, sibling or private grader access. Public check command: bun check.ts.
