Repair case/ErrorRow.tsx, exporting ErrorRow({rows,perform,labels}) with existing Props. Keep support/MobileSwipeRow.tsx and its dependency closure unchanged. Each row has a stable id; perform(id) is asynchronous. Rejection must retain/restore the correct row and display an accessible error plus retry. Reorder must preserve error identity. Success removes the row and clears its error. Each activation dispatches once; pending activation cannot duplicate it. Keep keyboard and touch swipe/tray actions usable. The list must scroll to its final row/error; no clipped or overlapping retry controls, horizontal overflow at most 1 CSS pixel, touch targets at least 44×44. Required viewports: 1440×900, 390×844, 390×600; English/Ukrainian, light/dark. Capture before action, after rejection and after successful retry. Edit only case/ErrorRow.tsx, case/error-row.css and case/ErrorRow.test.tsx. No history, siblings, grader, production or network access.
