Behavior: the final rejected action stays visible with an error and retry affordance on desktop and phone. Diagnosis: rendering discards the terminal row before identity reconciliation. Reuse the stable action identity. Maintain keyboard and touch access. Files: `case/ErrorRow.tsx`, `case/error-row.css`, and tests. Check desktop and phone geometry plus rendered pixels. Escalate if a stable action identity is unavailable.
