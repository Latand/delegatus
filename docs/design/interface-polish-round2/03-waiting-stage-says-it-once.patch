diff --git a/src/components/kanban/StageDraft.tsx b/src/components/kanban/StageDraft.tsx
index 3040698..480b766 100644
--- a/src/components/kanban/StageDraft.tsx
+++ b/src/components/kanban/StageDraft.tsx
@@ -26,7 +26,6 @@ import { draftFacts, draftOutcome, neverLaunched, pipelineEnded, stageDraftable,
 
 const PencilGlyph = () => <svg {...svgProps}><path d="M12 20h9" /><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4z" /></svg>;
 const ClockGlyph = () => <svg {...svgProps}><circle cx="12" cy="12" r="9" /><path d="M12 7v5l3 2" /></svg>;
-const SendGlyph = () => <svg {...svgProps}><path d="m5 12 14-7-5 14-2.5-5.5z" /></svg>;
 
 
 export function useStageDraft(drafts: StageDrafts, key: string): StageDraft | null {
@@ -59,18 +58,10 @@ export function StageDraftFeed({ pipeline, stage, names, drafts, ports }: {
         <div className="msg event"><i className="edot" aria-hidden="true" /><span>{when}</span></div>
         <StageDraftMessage pipeline={pipeline} stage={stage} name={name} drafts={drafts} ports={ports} />
         <AddedAtStart pipeline={pipeline} stage={stage} />
-        <p className="draft-note">
-          {ended
-            ? t("kanban.draft.noteEnded")
-            : facts.after ? t("kanban.draft.noteAfter", { stage: name, after: nameOf(facts.after) }) : t("kanban.draft.noteFirst", { stage: name })}
-        </p>
-      </div>
-      <div className="composer2 disabled" aria-disabled="true">
-        <div className="c-box">
-          <textarea className="c-field" rows={1} disabled aria-label={t("kanban.draft.composerAria", { stage: name })} placeholder={t("kanban.draft.composerPlaceholder", { stage: name })} />
-          <button type="button" className="c-send" disabled aria-label={t("kanban.draft.send")}><SendGlyph /></button>
-        </div>
-        <p className="c-status" role="status">{t("kanban.draft.composerNote")}</p>
+        {/* The line above already says when it starts, and the message's own
+            label says it is not sent. A stage that has not started has nothing
+            to reply to, so it shows no composer until it starts. */}
+        {ended ? <p className="draft-note">{t("kanban.draft.noteEnded")}</p> : null}
       </div>
     </>
   );
diff --git a/src/components/kanban/StagesSheet.tsx b/src/components/kanban/StagesSheet.tsx
index b03369b..585cdad 100644
--- a/src/components/kanban/StagesSheet.tsx
+++ b/src/components/kanban/StagesSheet.tsx
@@ -447,7 +447,8 @@ function StagePane(props: Parameters<typeof StagesSheet>[0] & {
           <MoreGlyph />
         </button>
       </div>
-      <div className="pane-sub">{bits}</div>
+      {/* A stage that never ran says when it starts once, in its draft. */}
+      {neverRan ? null : <div className="pane-sub">{bits}</div>}
       {attempts.length > 1 ? (
         <div className="attempts" role="group" aria-label={t("kanban.stages.attempts")}>
           {attempts.map((attempt) => {
diff --git a/src/components/kanban/kanbanBoard.css b/src/components/kanban/kanbanBoard.css
index 6d9863e..c6eef01 100644
--- a/src/components/kanban/kanbanBoard.css
+++ b/src/components/kanban/kanbanBoard.css
@@ -1110,7 +1110,8 @@
 .kb .draft-acts .btn { height: 28px; }
 .kb .bstatus { display: inline-flex; align-items: center; gap: 4px; font-size: var(--text-label); font-weight: 600; }
 .kb .bstatus svg { width: 12px; height: 12px; }
-.kb .bstatus.waiting { color: var(--color-warning); }
+/* An ordinary wait is not a warning: amber stays for what needs the operator. */
+.kb .bstatus.waiting { color: var(--color-muted); font-weight: 500; }
 .kb .bstatus.ended { color: var(--color-muted); }
 .kb .draft-note { margin: 0; font-size: var(--text-label); color: var(--color-muted); text-align: center; }
 .kb .bstatus.starting { color: var(--color-accent); }
diff --git a/src/lib/i18n/en.ts b/src/lib/i18n/en.ts
index 3feb7e3..eaed2bd 100644
--- a/src/lib/i18n/en.ts
+++ b/src/lib/i18n/en.ts
@@ -3634,7 +3634,7 @@ export const en = {
   "kanban.draft.editAria": "Edit the first message of {stage}",
   "kanban.draft.editTitle": "Edit before the stage starts",
   "kanban.draft.editMenu": "Edit the first message",
-  "kanban.draft.status": "Waiting for stage start · not delivered",
+  "kanban.draft.status": "First message · not sent yet",
   "kanban.draft.statusEnded": "Never delivered · the pipeline ended before this stage started",
   "kanban.draft.edited": " · edited {time}",
   "kanban.draft.onlyPrevOutput": "No words of its own: it starts from the previous stage's output.",
diff --git a/src/lib/i18n/uk.ts b/src/lib/i18n/uk.ts
index 82ec38f..856b6c6 100644
--- a/src/lib/i18n/uk.ts
+++ b/src/lib/i18n/uk.ts
@@ -3533,7 +3533,7 @@ export const uk: Record<keyof typeof en, Message> = {
   "kanban.draft.editAria": "Змінити перше повідомлення {stage}",
   "kanban.draft.editTitle": "Змінити, доки етап не стартував",
   "kanban.draft.editMenu": "Змінити перше повідомлення",
-  "kanban.draft.status": "Чекає старту етапу · не доставлено",
+  "kanban.draft.status": "Перше повідомлення · ще не надіслано",
   "kanban.draft.statusEnded": "Не доставлено · конвеєр завершився раніше, ніж етап стартував",
   "kanban.draft.edited": " · змінено о {time}",
   "kanban.draft.onlyPrevOutput": "Без власних слів: стартує з результату попереднього етапу.",
