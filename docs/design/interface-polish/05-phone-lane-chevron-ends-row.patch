diff --git a/src/components/pipelines/pipelineBlock.css b/src/components/pipelines/pipelineBlock.css
index 1a1efb3..c23b462 100644
--- a/src/components/pipelines/pipelineBlock.css
+++ b/src/components/pipelines/pipelineBlock.css
@@ -256,6 +256,10 @@
 .phone-lane .pblock .pb-title { grid-column: 1; white-space: normal; display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 2; font-size: var(--text-body); line-height: 1.3; overflow-wrap: anywhere; }
 .phone-lane .pblock .pb-meta { grid-column: 1; }
 .phone-lane .pblock .pb-open > svg { grid-column: 2; grid-row: 1; }
+/* The head button is the whole row here, so its chevron ends the row the way
+   the Description and agent rows below end theirs; the spacer that shares
+   the width on the board would leave it floating mid-card. */
+.phone-lane .pblock .pb-head > .pb-grow { display: none; }
 /* A chip at the end of the chain reaches 44 px like the pills beside it: it
    sits on the pills' middle, so both reaches stop at the same line. */
 @media (pointer: coarse) {
